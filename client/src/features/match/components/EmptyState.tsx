export const EmptyState = () => {
  return (
    <div className="flex items-center justify-center h-full text-center text-2xl font-bold">
      <p>You've swiped through every molecule in range — the lab's quiet for now</p>
    </div>
  );
};
